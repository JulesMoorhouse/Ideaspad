Public Module Overlap
    'added so I don't have to give you lots of my existing code
    Public AppExe As String
    Friend Function GetWrittenCRC(ByVal pstrFileToCheck As String) As Integer

    End Function
    Function Decrypt(ByRef pstrInp() As String, Optional ByVal BugWork As Integer = 0) As String

    End Function
End Module
